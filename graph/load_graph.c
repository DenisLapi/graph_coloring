#include <stdio.h>
#include "./load_graph.h"

void load_graph(int *graph_matrix, int num_of_nodes) {

	// *graph_matrix - In this pointer (external) we will add the data from our file
	// num_of_nodes  - In this variable (external) we will save the number of nodes

	int *graph_matrix_init = graph_matrix;
	int option;

	printf("Which file (.txt) do you want to load from \"data\" folder:\n");
	printf("[!] Enter the file name without extension: ");

}